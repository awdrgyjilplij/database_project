<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.databaseproject">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DatabaseProject"
        android:usesCleartextTraffic="true">
        <activity android:name=".AdminActivity"></activity>
        <activity
            android:name=".BranchShowActivity"
            android:label="@string/title_activity_branch_show"
            android:theme="@style/Theme.DatabaseProject.NoActionBar" />
        <activity android:name=".SearchActivity" />
        <activity
            android:name=".EmployeeShowActivity"
            android:label="@string/title_activity_employee_show"
            android:theme="@style/Theme.DatabaseProject.NoActionBar" />
        <activity
            android:name=".SignupActivity"
            android:label="@string/title_activity_signup" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>